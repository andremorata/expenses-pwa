import { Component } from '@angular/core';
import { NavController } from 'ionic-angular';

@Component({
  selector: 'page-version-notes',
  templateUrl: 'version-notes.html'
})
export class VersionNotes {
  constructor(public navCtl: NavController) { }
}
